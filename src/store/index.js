import { createStore } from "vuex";

const store = createStore({
  state() {
    return {
      // scroll: null,
    };
  },
  mutations: {
    /* getScroll(state, scroll) {
      state.scroll = scroll;
    }, */
  },
});

export default store;
